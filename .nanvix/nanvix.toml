[package]
name = "libxml2"
version = "2.12.9"
nanvix-version = "0.12.511"

[builds]
[builds.matrix]
platforms = ["microvm"]
modes = ["multi-process", "single-process", "standalone"]
memory = ["256mb"]

[dependencies]
zlib = "1.3.1"
