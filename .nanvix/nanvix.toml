[package]
name = "libxml2"
version = "2.12.9"
nanvix-version = "0.13.3"

[builds]
[builds.matrix]
platforms = ["hyperlight", "microvm"]
modes = ["multi-process", "single-process", "standalone"]
memory = ["256mb"]

[dependencies]
zlib = "1.3.1"
